@page "/login/criar-senha/{UserName}"

<Niu.Nutri.Shared.Blazor.Authentication.Client.Components.CreatePassword UserName="@this.UserName" TokenValue="@this.Token" />

@code {
    [Parameter] public required string UserName { get; init; }
    [SupplyParameterFromQuery] public required string Token { get; set; }
}